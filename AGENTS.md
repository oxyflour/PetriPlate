# general
- **do not look into files in other workspaces or modify them**
- files are all encoded in utf8
- use proxy HTTPS_PROXY=http://proxy.yff.me:8124
- use `volta` and `pnpm` workspace for `nodejs`. remember to use `npx pnpm`. we always prefer using `TypeScript`. use `nextjs` App router for frontend development
- use `uv` for `python`. always create lock files
